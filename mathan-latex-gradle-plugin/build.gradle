plugins {
    id "com.gradle.plugin-publish" version "0.10.0"
    id "java-gradle-plugin"
}

repositories {
    jcenter()
}

version = "1.0.5"
group = "io.mathan.maven"

gradlePlugin {
    plugins {

    }
}