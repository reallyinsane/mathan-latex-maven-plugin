version = '1.0.1'

buildscript {
    repositories {
        mavenLocal()
        mavenCentral()
    }
    dependencies {
        classpath group: 'io.mathan.maven', name: 'mathan-latex-gradle-plugin',
                version: '1.0.1'
    }
}
apply plugin: 'io.mathan.latex'

latex {
    outputFormat = 'ps'
}